rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allowing all access since authentication was removed from the app
      allow read, write: if true;
    }
  }
}
